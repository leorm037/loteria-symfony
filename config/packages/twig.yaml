twig:
    form_themes: ['bootstrap_5_layout.html.twig']
    file_name_pattern: '*.twig'
    
    paths:
        '%kernel.project_dir%/assets/styles': assets_styles

when@test:
    twig:
        strict_variables: true
